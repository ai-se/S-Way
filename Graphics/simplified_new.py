from __future__ import division
import os

def for_normalizing(files):
    """
    @param files: All the files related to a problem
    @return: max and mix for each objective
    """


def find_files(problem, pop_size):
    data_folder = "./RawData/PopulationArchives/"
    folders = [d for d in os.listdir(data_folder) if problem.name in d and ]
    add population number
    import pdb
    pdb.set_trace()


def get_performance_measures(problem):
    find_files(problem)